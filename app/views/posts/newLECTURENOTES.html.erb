<p>Creating a New Post</p>

<%= render 'form' %>

<!-- everything below gets extracted to partial _form.html.erb 

<h5>Pure HTML form</h5>
<form action='/posts' method='POST'>
Title: <input type='text'>
<br/>
<input type='submit'>
</form>

<h4>Rails form Helpers</h4>
<%= form_tag '/posts' do %>
	<%= label_tag :title, "Input a title" %>
	<%= text_field_tag :title %>
	<br/>
	<%= label_tag :whatever %>
	<%= text_field_tag :whatever %>
	<br/>
	<%= submit_tag "Create Post", class: 'btn btn-primary' %>
<% end %>

<h4>Rails model-backed form helpers</h4>

<% if @post.errors.any? %>
	<div class='alert alert-error'>
	<h5>There were soome errors:</h5>
	<ul>
		<% @post.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
	</ul>
	</div>
<% end %>

<%= form_for @post do |f| %>  <!--  actually is PATCH /posts/:id -->
	<%= f.label :title %>
	<%= f.text_field :title %>
	<br/>
	<%= f.label :url %>
	<%= f.text_field :url %>
	<br/>
	<%= f.submit "Create Post", class: 'btn btn-primary' %>
<% end %>

-->