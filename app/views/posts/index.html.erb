<%= render 'shared/content_title', title: "All Posts" %>

<% if logged_in? %>
	[<%= link_to 'edit', edit_post_path(post) %>]
<% end %>

<% @posts.each do |post| %>

	<%= render 'post', post: post %>

<% end %>